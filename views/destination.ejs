<!-- views/destination.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= place %> - Smart Travel Planner</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css" />
</head>
<body class="bg-[#f9f9f9] text-gray-900">

  <div class="max-w-5xl mx-auto px-4 py-12">
    <h1 class="text-4xl font-bold text-blue-800 mb-4"><%= place %></h1>

    <img src="<%= image %>" alt="<%= place %> image" class="rounded-lg w-full max-h-[400px] object-cover shadow mb-6">

    <section class="mb-8">
      <h2 class="text-2xl font-semibold text-blue-700 mb-2">📍 Speciality of <%= place %></h2>
      <p class="text-gray-700 leading-relaxed"><%= description %></p>
    </section>

    <section class="mb-8">
      <h2 class="text-2xl font-semibold text-green-700 mb-2">💰 Budget Estimate</h2>
      <ul class="list-disc pl-6 space-y-1">
        <li>✈️ Flight: <%= budget.flight %></li>
        <li>🏨 Hotel: <%= budget.hotel %></li>
        <li>🍱 Food: <%= budget.food %></li>
        <li>🚖 Transport: <%= budget.transport %></li>
        <li>🎟️ Activities: <%= budget.activities %></li>
      </ul>
    </section>

    <section class="mb-8">
      <h2 class="text-2xl font-semibold text-indigo-700 mb-2">🗺️ Live Location</h2>
      <iframe class="w-full rounded shadow" height="300" frameborder="0" src="<%= map %>" allowfullscreen loading="lazy"></iframe>
      <a href="<%= mapLink %>" target="_blank" class="inline-block mt-3 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">Open in Google Maps</a>
    </section>

    <section class="mb-8">
      <h2 class="text-2xl font-semibold text-purple-700 mb-4">💬 Reviews</h2>
      <form action="/review/<%= place.toLowerCase() %>" method="POST" class="space-y-4 mb-6">
        <input type="text" name="name" placeholder="Your Name" required class="w-full p-3 border rounded">
        <select name="rating" required class="w-full p-3 border rounded">
          <option value="">Rate your experience (1 to 5)</option>
          <option value="1">⭐</option>
          <option value="2">⭐⭐</option>
          <option value="3">⭐⭐⭐</option>
          <option value="4">⭐⭐⭐⭐</option>
          <option value="5">⭐⭐⭐⭐⭐</option>
        </select>
        <textarea name="review" placeholder="Write your review..." required class="w-full p-3 border rounded resize-none"></textarea>
        <button type="submit" class="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700">Submit Review</button>
      </form>

      <% if (reviews.length === 0) { %>
        <p class="text-gray-500">No reviews yet. Be the first to write one!</p>
      <% } else { %>
        <ul class="space-y-3">
          <% reviews.forEach(r => { %>
            <li class="bg-gray-100 p-4 rounded shadow">
              <p class="font-semibold text-blue-800"><%= r.name %> <span class="text-yellow-500">(<%= '⭐'.repeat(r.rating) %>)</span></p>
              <p class="mt-1 text-sm text-gray-800">"<%= r.comment %>"</p>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </section>
  </div>

</body>
</html>